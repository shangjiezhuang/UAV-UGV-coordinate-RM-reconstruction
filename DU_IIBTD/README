用 Deep unfolding 思想加速 IIBTD 算法的求解。当前训练方式是 set-based reconstruction：
每个训练样本一次性输入多个离散采样点及其频段观测，然后输出整张重建图。

当前实现先固定 R=1，用 RadioSeerDPM100PSD 做算法测试和训练：

S = {Theta, Phi, Sr, H_hat}

每次输入一个离散观测集合：

z: 多个采样点位置
gamma: 每个采样点的频段观测
Omega: 每个采样点的观测掩码

网络功能：

(Theta, Phi, Sr, H_hat) =
f_du_iibtd(S_init, z_set, gamma_set, Omega_set)

实现要点：

- Theta 使用 IIBTD_Opt_GPU 中 R=1 的 batched WLS 思路，在 GPU 上批量求解。
- Phi 使用 R=1 非负闭式更新，并带可学习 blend。
- Sr 使用 CNN proximal block 替代原 SVT/SVD 步骤。
- 训练集默认每个 epoch 重新采样离散观测集合，val/test 观测集合固定用于稳定评估。
- 训练入口默认要求 CUDA。
- 训练过程会在输出目录保存 history.json、training_curves.png 和最终 best checkpoint 的 test_metrics.json。
- 如果不显式指定 --output-dir，会在当前工作目录下创建 YYYYMMDD_HHMMSS 格式的结果目录。

GPU smoke train:

python DU_IIBTD/train.py --device cuda --max-samples 2 --epochs 1 --spatial-points-min 8 --spatial-points-max 8 --T 1 --hidden 8 --batch-size 1 --output-dir /tmp/du_iibtd_smoke

正式训练建议先从较轻的 mini-epoch 设置开始。这里 `train-samples-per-epoch` 表示每个 mini-epoch 只训练一部分样本，多个 mini-epoch 循环覆盖完整训练集：

python DU_IIBTD/train.py --device cuda --max-samples 0 --epochs 30 --train-samples-per-epoch 1000 --val-interval 10 --batch-size 2 --num-workers 2 --spatial-points-min 128 --spatial-points-max 256 --T 3 --hidden 32
